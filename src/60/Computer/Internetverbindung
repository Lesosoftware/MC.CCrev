shell.run("clear")
print("Folgende Verbindungen stehen zu Verfuegung")

number=1

while router>0 then
  write("(")
  write(number)
  write(") ")
  number=number+1
  print(os.getRouterLabel)
end
print("")
